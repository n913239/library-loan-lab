紅的只有一條:`晚 1 秒 → 1`(上一個 commit `4df4ea3` 改的)。動手前要先確認一件事,因為這條新期望跟三處現有東西打架:

1. **`docs/spec.md` 決定 3** 明寫 `floor`、「未滿一天不算」。
2. **測試檔自己的檔頭註解**(第 3 行)也寫 `floor`。
3. **同一個測試檔的 `晚 3 天又 23 小時 → 3`**。如果「未滿一天也算一天」(commit message 的說法),這條應該是 4,但它還是 3、而且目前是綠的。

六條 case 要全綠,唯一能湊出來的規則是「只要逾期 > 0 秒就至少算 1 天,之後照 floor」—— 這個規則不在 spec、不在測試註解、也不在 commit message 裡,等於我幫你憑空定一條新的規格決定。三種走法的產出完全不同,所以先問:

- **A. 測試是對的,規格要改** → 我改 `overdue.js` 成 `max(1, floor)`,同時改 spec 決定 3 和測試檔頭註解;但你要先確認 `3天23小時 → 3` 真的是你要的(不是 4)。
- **B. 真的是「未滿一天算一天」= ceil** → 改 `overdue.js` 成 ceil,`3天23小時` 那條改成 4,spec 決定 3 跟著改。
- **C. 上一個 commit 改錯了** → revert `4df4ea3`,程式碼不動。

我的建議是 **C**:spec 決定 3 的理由(「逾期只記錄不收錢,寬鬆沒有成本」)是有意識的取捨,而且只改了一條 case 沒改鄰居,看起來像手滑。你選哪一個?