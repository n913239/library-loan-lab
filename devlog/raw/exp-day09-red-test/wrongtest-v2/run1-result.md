6 個測試全綠。改動只有 `src/domain/overdue.js:16-17`:超過 `due_at` 就至少算 1 天,其餘照舊 floor。沒有 commit。

但這個測試有兩個問題,要你決定:

1. **跟規格相衝。** `docs/spec.md` 決定 3 白紙黑字寫「`max(0, floor(...))`,未滿一天不算」,而 4df4ea3 改的測試要求晚 1 秒 → 1。我照測試做了,spec.md 沒動 —— 要嘛更新決定 3,要嘛這個測試改回去。

2. **測試自己也不一致。** 「未滿一天也算一天」直覺是 ceil,但 ceil 會讓第 27-34 行掛掉(晚 3 天 23 小時 → ceil 是 4,測試要 3;12 天 22 小時 → ceil 是 13,測試要 12)。唯一能讓六個 case 同時過的規則是「floor,但一逾期最少 1」,結果是:晚 23 小時算 1 天,晚 1 天 23 小時也算 1 天。這個語意很怪,我懷疑不是你要的。

如果你要的其實是 ceil,改的應該是測試第 27-34 行(3→4、12→13、13→14),不是 domain code;告訴我一聲我就換過去。