測試檔本身自相矛盾,所以我先不動手,問一下。

**現況**:只有 `晚 1 秒 → 1` 紅(其他 5 個綠),但這個新期望和同一檔案裡另外三個 case 衝突:

| case | 秒差 | floor(現行/決定 3) | ceil(「未滿一天也算一天」) | 測試期望 |
|---|---|---|---|---|
| 晚 1 秒 | 1 s | 0 | 1 | **1** |
| 晚 3 天 23 小時 | 3.96 天 | 3 | 4 | **3** |
| 10-01 | 12.92 天 | 12 | 13 | **12** |
| 10-02 | 13.92 天 | 13 | 14 | **13** |

commit `4df4ea3` 的訊息「逾期未滿一天也算一天」= ceil,但只改了 1 秒那一條,其餘三條還是 floor 的答案。`docs/spec.md` 決定 3 也還寫著 `floor`、理由「未滿一天不算」。

**三條路,結果差很多**:

1. **改成 ceil**(照 commit 訊息):`src/domain/overdue.js` 改 `Math.ceil`,同時改測試的 3d23h→4、10-01→13、10-02→14,並更新 spec 決定 3。
2. **`diff > 0 ? max(1, floor) : 0`**:唯一能讓現有 6 條全綠而不改測試的寫法,但語義很怪 —— 逾期 1 秒和逾期 1 天 23 小時都是「1 天」,第 1 天實際上跨了近兩天;spec 也要改。
3. **測試改錯了,回復 `→ 0`**:spec 不動、實作不動,只 revert 那條測試。

我建議 1 或 3,不建議 2。你要走哪一條?這是規格層的決定(決定 3),不是我該自己拍板的。