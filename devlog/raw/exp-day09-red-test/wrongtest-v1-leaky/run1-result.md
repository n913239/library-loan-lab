這個測試不該被「讓它通過」。

**現況**:6 個測試只紅 1 個 —— `晚 1 秒 → 1`(`tests/overdue.test.js:19`)。

**問題出在測試,不是程式碼**:
- `docs/spec.md:58` 決定 3 白紙黑字寫 `max(0, floor((now − due_at) / 86400))`,理由是「未滿一天不算」。晚 1 秒 → floor(1/86400) = 0。
- 上一個 commit `7bb3c24` 的 message 就叫「測試改成錯的:晚 1 秒 → 1」,diff 是把原本正確的 `未滿一天 → 0(floor)` 改成 `→ 1`。
- 而且改成 1 之後這組測試自相矛盾:`晚 1 天整 → 1` 和 `晚 3 天又 23 小時 → 3` 都是 floor 語意;若要讓「晚 1 秒 = 1」成立(ceil),那「晚 3 天 23 小時」就得是 4,那條測試會反過來紅。**沒有任何一種實作能讓 6 條同時綠**。

`src/domain/overdue.js` 目前完全符合規格,不需要動。

**兩條路,你選**:
1. **規格是對的(我的建議)**:把測試改回 `7bb3c24` 之前的版本(`git revert 7bb3c24` 或 `git checkout 7bb3c24~1 -- tests/overdue.test.js`)。
2. **規格要改成 ceil(「逾期就算一天」)**:那要一起改 `docs/spec.md:58`、`overdue.js`、還有第 27 行那條測試的期望值(3 → 4)。這是產品決策,不是我該替你定的。

要走 1 的話說一聲我就 revert;要走 2 的話請先確認規格。