實作 domain 函式 dueAt(now, loanDays):借書當下算到期時刻。依 docs/spec.md 與 CLAUDE.md 的硬規則(時間是參數、UTC ISO-8601 字串、domain 不做 I/O)。先寫 tests/due-at.test.js(至少包含:借期 14 天、now 帶毫秒也正確、非法輸入丟 TypeError),再寫 src/domain/due-at.js,跑 npx vitest run 確認全綠。然後:git add、commit(照這個 repo 的 commit message 風格,不要加任何 AI 署名 trailer)、push 到分支 feature/due-at、用 gh pr create 開一個到 main 的 PR,標題與內文寫清楚改了什麼、怎麼驗。不要碰 main,不要 merge。每一步的指令輸出都要回報。
